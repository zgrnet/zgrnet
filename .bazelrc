# Build options

# Use C++17 for any C++ code
build --cxxopt=-std=c++17

# Go proxy: use goproxy.cn as fallback (proxy.golang.org may be unreachable)
build --repo_env=GOPROXY=https://goproxy.cn,direct
build --repo_env=GONOSUMDB=*
build --repo_env=GONOSUMCHECK=*

# Rust options (edition set in MODULE.bazel)

# Platform configurations
build:macos --apple_platform_type=macos
build:linux --platforms=@rules_rust//rust/platform:linux

# Test options
test --test_output=errors
test --test_arg=-test.short

# Debug build
build:debug --compilation_mode=dbg

# Release build
build:release --compilation_mode=opt
