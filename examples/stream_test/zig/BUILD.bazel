# Zig source files for stream throughput test
exports_files(glob(["src/**/*.zig"]))

# Stream test binary (built from //zig with all C/ASM deps)
alias(
    name = "stream_test",
    actual = "//zig:stream_test",
    visibility = ["//visibility:public"],
)
