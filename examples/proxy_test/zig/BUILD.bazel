# Zig source files for proxy interop test
exports_files(glob(["src/**/*.zig"]))

# Proxy test binary (built from //zig with all C/ASM deps)
alias(
    name = "proxy_test",
    actual = "//zig:proxy_test",
    visibility = ["//visibility:public"],
)
