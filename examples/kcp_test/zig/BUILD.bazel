# Zig source files for KCP interop test
exports_files(glob(["src/**/*.zig"]))

# KCP interop test binary (built from //zig with all C/ASM deps)
alias(
    name = "kcp_test",
    actual = "//zig:kcp_test",
    visibility = ["//visibility:public"],
)
