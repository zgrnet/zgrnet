[package]
name = "kcp_test"
version = "0.1.0"
edition = "2021"

[dependencies]
zgrnet = { package = "zgrnet-noise", path = "../../../rust" }
hex = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
