# Zig throughput test
# Usage: bazel run //examples/throughput/zig:run

load("//bazel/zig:defs.bzl", "zig_run")

exports_files(["main.zig"])

zig_run(
    name = "run",
    srcs = glob(["**/*"]) + ["//zig:srcs"],
    build_args = ["throughput", "-Doptimize=ReleaseFast"],
    zig_root = "zig",
)
