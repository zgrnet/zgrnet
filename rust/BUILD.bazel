load("@rules_rust//rust:defs.bzl", "rust_library", "rust_test")
load("@crates//:defs.bzl", "all_crate_deps")

# Rust SDK - Noise Protocol implementation
rust_library(
    name = "noise",
    srcs = glob(["src/**/*.rs"]),
    crate_name = "noise",
    edition = "2021",
    visibility = ["//visibility:public"],
    deps = all_crate_deps(),
)

rust_test(
    name = "noise_test",
    crate = ":noise",
    deps = all_crate_deps(normal_dev = True),
)
