# 概述

ZigNet 是一个去中心化的网络层，以公钥作为唯一身份标识。它基于 Noise Protocol 实现端到端加密，支持 NAT 穿透和多跳中继，不依赖传统的 IP 地址分配和 DNS 系统。

## 核心特性

- 公钥即身份，无需注册，自主生成
- IP 地址是本地概念，不同节点看到的 IP 可以不同
- 支持直连和中继两种连接方式
- 可插拔的身份验证策略（白名单、链上验证等）
- 端到端加密，中继节点无法解密内容


