# Zig throughput test
# Note: Previously used zig_run (build.zig based). Now just exports the source
# file for the //zig:noise library's build.zig to reference in local dev.
# For Bazel, a proper zig_binary target can be added here when needed.

exports_files(["main.zig"])
